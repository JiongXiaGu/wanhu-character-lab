# V2 人体拓扑规范（历史记录，已由 V3 替代）

这份路径保留用于旧对话链接与迁移说明，不再描述当前实现。

V2 采用前后轮廓挤出和独立的 planar vertex/triangle 结构。用户指出形体、关节面流和美术效果不足，现已替换为 src/character/v3 的固定三维、四边面主导连续人体。

V3 保留的原则：运行时生成；低面数；稳定骨架和参数语义；衣物替换覆盖区域；没有实时布料模拟；必须实看三视图与动作截图。

V3 不再遵循的旧规则：只做固定裸模就停止；旧版 452 tris / 228 verts 指标；二维轮廓向前后复制；在用户确认每个中间步骤前不做任何基本动作。当前用户已授权推进到人物+基础动作可验收。

当前规范见 工作交接.md、运行时人物生成架构.md、服装生成架构.md 和 V3验收记录.md。旧完整文本可在 Git 历史中查阅。
