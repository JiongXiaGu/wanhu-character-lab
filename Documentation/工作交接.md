# 工作交接 · 衣冠工坊 V2

先读 README、服装动画适配V2、换装工作台使用、服装生成架构、GPU迁移契约与AGENTS。定位不变：玩家自定义优先、写意服饰、Web只换装/试衣；居民玩法在Unity。

## 版本沿革

男性基线6627c2e，女性V3.6，V1衣冠工坊e6cd770；用户在5d07b439补充动画。PR #10 的双层分裳在坐姿专项失败，不可将其“尚未审查”文档或测试结果当作通过。V2从用户最新main重新开发，不合入那套失败壳体。

## 当前结构

Recipe V4七槽位/染色/发式 → v3/outfit → wardrobe/tailoring连续衣面 → CharacterData → 20骨骼蒙皮。原始男女人体、男性12份几何哈希、骨架和头部校准不变。服装在克隆的可见表面上修改裆点、放量、髋膝过渡；不同时画两层腿/裙壳，不删整段腿。

裙装现在是裤式分裳的写意近似，不是带悬垂布料的连筒裙；显示名与文档必须承认取舍。原稳定部件ID和旧配方保留。写意版型仍需真实动画审图，不宣传任意姿态零穿模。

## FBX与测试库

prepare:mixamo递归扫描实际FBX，生成catalog.generated.ts；旧11ID保留，新文件进入搜索/分类/收藏面板。生成目录后动态import提取器，避免加载旧注册表。inventory.json为对象，包含totalFiles、prepared、clips、failures。缓存核对源SHA与extractorVersion。

新文件只要满足原Mixamo骨架与真实bind要求即可自动进入面板；不把源人物mesh/贴图带入游戏。未知片段默认单次。Snatch归劳动测试供搬举压力验证，不等同于完整生产动作。需要锄地、推车等时另补真实FBX。

## LOD

0/1/2仅控制服饰细分，不写入Recipe。主衣面闭合，禁止T接缝，保留同骨架/当前相位。仍保留510tris源人体；不是完整低模人群。动画LOD、自动屏幕占比切换、剔除与GPU批量绘制另做。

## 验收及待办

五条Actions，新增Tailoring V2覆盖144静态组合、全目录实际加载、三样板男女三级LOD坐姿多视图、完整时间采样与视频。数字/数量以本次报告为准。生成脚本不填写manuallyReviewed=true；人工必须实际看图后记录具体SHA和结论。

原男体基线禁止自动刷新。常用动作有明显问题先修再合main。仅文档提交可以复用未变化父代码报告。PR/合并状态以GitHub实际读取为准，V2验收状态记录在服装动画适配V2.md。

后续重点：在稳定版型上改善审美，补真正劳动FBX，Unity单人一致性验证后再做共享缓存/参数染色/人群性能。不要在Web添加职业、财富、生产和居民AI。
