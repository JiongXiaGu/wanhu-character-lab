# 工作交接 · 低模形变修正

## 当前任务

项目为 `JiongXiaGu/wanhu-character-lab` 的3D玩家/居民换装，不是头像或UI原型。第一批独立衣裤已由PR #14合入。当前延续原 `fix/lowpoly-deformation-cages` 分支的膝部与裸模裆底修正，PR #15；未另建平行V5。

接手先读远端最新main和PR状态，再读《膝部与裆底修正.md》《GitHubActions截图验收规范.md》及AGENTS。不得用本文的历史SHA覆盖后续工作。

## 已接纳的最新文档

main `734f52c7c4a000a4aa8174d28147ef214777825a` 的四份本地审图流程文档已合入任务分支。日常优先真实本地网页截图；静态源码几何重建只作明确标注的静态辅助，不能验证FBX或页面。网格/蒙皮改动准备合main仍核对正式Actions，不要求每个中间美术提交都等完整CI。

本次收尾对话已经可直接写入GitHub并下载Actions产物。已实际打开代表性图片，包括男女裸模静态、男性慢跑线框、Wardrobe慢跑、Tailoring坐姿正侧面，以及Modular的Pilot Flips Switches前后对照和髋裆近景。抽查未见缺面、爆炸式形变或旧式坐姿长尖片；这仍是代表性图片抽样，不等于逐帧人工检查所有动画。

## 已提交代码

- 裸模膝部位置与低模轮廓保留，膝后改为按深度放宽的静态双权重梯度；裤装共用关节制作基准，并删除独立膝后压薄、长权重过渡和冗余环。
- 人体由510→524tris，用于有限宽度骨盆/裆底；单条裤装252→220tris。仍为20骨骼/最多双权重。
- 删除旧单一中央Crotch连接，皮肤骨盆改为左右8点腿根与有限宽度裆底，再过渡到原6点大腿；不是动作相位驱动的补偿。
- 调试布线显示真实三角对角线；FBX、参考模型、灯光、retarget、覆盖区域、Recipe V5和骨架绑定不变。
- 固定绑定基线不刷新；旧whole-body哈希在骨盆拓扑升级后退役。新的保护区域签名继续锁定未声明位置、面、锚点与非腿部权重，并保留3个故障注入反例。
- 新增 `npm run review:local`，自动提取FBX、启动临时本机Vite、快速32张/完整156张截图、退出清理进程。真实执行结果待下述正式检查核实；不能把runner的命令烟雾测试写成本对话本地跑通。

服装几何v4、皮肤几何v3、骨架绑定v1。体型仍只有固定成年男女；儿童老人为后续独立制作，不引入连续体型或实时求解器。

## 清理与正式检查

一次性snapshot/candidate工作流和两个临时应用/发布脚本已删除。保留六条正式Actions；裸模/裤装完整对照并入Modular Garment Review，不增第七条永久任务。新 `check:deformation` 纳入 `check:mesh`；贯穿检测增加裸模，原七款裤装/重点动作/源键中点/算法容差均保留。

运行时代码受测HEAD为 `53a8ebc07772591fb69851539ca86015c2897f80`。六条正式Actions均通过：Build #147、Character Model Review #79、Mixamo Retarget Review #66、Wardrobe Review #59、Tailoring V2 Review #43、Modular Garment Review #14。后续若只有本文等纯文档同步，最终HEAD可不同，但必须继续把受测代码SHA与最终文档SHA分开记录。

## 阶段结论与后续

新版膝盖与裸模裆底已经完成正式自动检查，并实际抽查关键浏览器截图。重点Pilot Flips Switches、Snatch、Jogging、Shooting Arrow等仍由Actions保留连续回归；当前结论是达到“可拉取稳定低模修复版”，不是宣称所有连续瞬间零穿插。

PR #15作为本阶段交付合入main；原PR #12失败补面不合入。下一阶段转入更有轮廓差异的衣服和装饰，不重新把完美裸模当整个衣柜的前置条件。

## 本地体验

合并前：`git fetch origin`、`git switch fix/lowpoly-deformation-cages`、`git pull --ff-only origin fix/lowpoly-deformation-cages`、`npm ci`、`npm run dev`。
Node.js>=22.12，打开终端地址。快速审图另需一次 `npx playwright install chromium`，随后 `npm run review:local`；完整模式加 `-- --full`。合并后再切回main。截图保存在review-deformation/local，不进入源码库。
