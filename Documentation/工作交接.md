> **V2.1 单一标准模型更新（本轮实施，实际验收见《标准低模髋裆修正》）：** 当前LOD2升级为唯一标准精度，旧LOD0/LOD1细分路径与UI已删除。保留旧lod参数仅作归一兼容，不产生另一个网格。衣面新增共享索引髋根环和四角裆底，仅局部平滑法线；源人体/20骨骼/双权重/FBX/Recipe V4不变。下文旧LOD数量和旧裆点结构只属于历史版本，不作为当前实现。

# 工作交接 · 衣冠工坊 V2

先读 README、服装动画适配V2、换装工作台使用、服装生成架构、GPU迁移契约与AGENTS。定位不变：玩家自定义优先、写意服饰、Web只换装/试衣；居民玩法在Unity。

## 版本沿革

男性基线6627c2e，女性V3.6，V1衣冠工坊e6cd770；用户在5d07b439补充动画。PR #10 的双层分裳在坐姿专项失败，V2从用户最新main重新开发，不合入那套失败壳体。

PR #11 的运行代码 `c7b98b6204bf7a797dc85ae08742ac3f49a4c2f2` 已通过五条Actions，并完成下载、哈希核对和实际图像/完整视频帧序列审查。具体Run ID、计数、范围、限制见服装动画适配V2.md。最后交付提交仅改文档；PR和main最终SHA应现场读取，不能将这里的受测代码SHA当作合并SHA。

## 当前结构

Recipe V4七槽位/染色/发式 → v3/outfit → wardrobe/tailoring连续衣面 → CharacterData → 20骨骼蒙皮。原始男女人体、男性12份几何哈希、骨架和头部校准不变。服装在克隆的可见表面上修改裆点、放量、髋膝过渡与体格分级褶窝，不同时画两层腿/裙壳，不删整段腿。

裙装现在是裤式分裳的写意近似，不是带悬垂布料的连筒裙；显示名、目录图标与文档必须承认取舍。原稳定部件ID和旧配方保留。近景仍有髋下暗面、薄大腿侧面和膝前硬折，需要在贯穿门槛内改善造型，不宣传任意姿态零穿模。

## FBX与测试库

prepare:mixamo递归扫描实际FBX，生成catalog.generated.ts；当前23份，旧11ID保留，新文件进入搜索/分类/收藏面板。中文分类词也参与搜索。生成目录后动态import提取器，避免加载旧注册表。inventory.json包含totalFiles、prepared、clips、failures；缓存核对源SHA与extractorVersion。

新文件必须满足Mixamo骨架与真实bind要求；不把源人物mesh/贴图带入游戏。未知片段默认单次。Snatch归劳动测试供搬举压力验证，不等同于完整生产动作。需要锄地、推车等时另补真实FBX。

## LOD

0/1/2仅控制服饰细分，不写入Recipe。主衣面闭合，禁止T接缝，保留同骨架/当前相位。仍保留510tris源人体；不是完整低模人群。动画LOD、自动屏幕占比切换、剔除与GPU批量绘制另做。

## 验收与后续

五条Actions；V2专项401图/8视频、144静态组合、7506有限值采样。独立腰髋/腿部贯穿门槛：坐姿、射箭、慢跑、Snatch、StartWalking，男女×三样板×三LOD×五组身材，全部源键与中点，90,810姿态。扫描线加速不放宽判定，检测范围不包括所有共面/相邻折叠、袖子和道具，实际审图仍必须做。

原男体基线禁止自动刷新。常用动作明显问题先修再合main。仅文档提交可以复用未变化父代码报告。脚本不自动填写manuallyReviewed=true。Actions必须安装ffmpeg，数字失败仍抓取诊断截图但不得覆盖失败状态。

后续重点：在稳定版型上改善审美，补真正劳动FBX，Unity单人一致性验证后再做共享缓存/参数染色/人群性能。不要在Web添加职业、财富、生产和居民AI。记录中途失败版本与最终通过版本的区别，不重新合入PR #10。
