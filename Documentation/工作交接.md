# 工作交接 · 低模形变修正

## 当前任务

项目为 `JiongXiaGu/wanhu-character-lab` 的3D玩家/居民换装，不是头像或UI原型。第一批独立衣裤已由PR #14合入。当前延续原 `fix/lowpoly-deformation-cages` 分支的膝部与裸模裆底修正，PR #15；未另建平行V5。

接手先读远端最新main和PR状态，再读《膝部与裆底修正.md》《GitHubActions截图验收规范.md》及AGENTS。不得用本文的历史SHA覆盖后续工作。

## 已接纳的最新文档

main `734f52c7c4a000a4aa8174d28147ef214777825a` 的四份本地审图流程文档已合入任务分支。日常优先真实本地网页截图；静态源码几何重建只作明确标注的静态辅助，不能验证FBX或页面。网格/蒙皮改动准备合main仍核对正式Actions，不要求每个中间美术提交都等完整CI。

当前对话的执行容器、Python与图片打开均返回TransportTimeoutError，因此没有实际完成ChatGPT本地运行或新版人工审图。下载不等于查看，数值通过不等于视觉通过。不得沿用以前错误的“已看完”说法。

## 已提交代码

- 裸模膝盖原值保留，裤装与之共用关节制作基准，删除独立裤装膝后压薄、长权重过渡和冗余环。
- 人体510tris不增；单条裤装252→220tris。仍为20骨骼/最多双权重。
- 裸模中央裆点由骨盆刚性改为双大腿等权候选。没有重建裸体髋裆拓扑；观感必须看新版坐姿/深蹲验证。
- 调试布线显示真实三角对角线；FBX、参考模型、灯光、retarget、覆盖区域、Recipe V5和骨架绑定不变。
- 原黄金文件不刷新。新增“唯一声明权重差异”的反向比较副本检查，其余身体与完整绑定保持原哈希保护，并有3个故障注入反例。
- 新增 `npm run review:local`，自动提取FBX、启动临时本机Vite、快速32张/完整156张截图、退出清理进程。真实执行结果待下述正式检查核实；不能把runner的命令烟雾测试写成本对话本地跑通。

服装几何v3、皮肤几何v2、骨架绑定v1。体型仍只有固定成年男女；儿童老人为后续独立制作，不引入连续体型或实时求解器。

## 清理与正式检查

一次性snapshot/candidate工作流和两个临时应用/发布脚本已删除。保留六条正式Actions；裸模/裤装完整对照并入Modular Garment Review，不增第七条永久任务。新 `check:deformation` 纳入 `check:mesh`；贯穿检测增加裸模，原七款裤装/重点动作/源键中点/算法容差均保留。

本次提交的六条正式Actions结果以PR #15最新记录为准。PR创建时受测HEAD `ea8a569cce84bc6b088d9468d86fa9afd9b74366`；后续只有文档时可引用该代码结果，必须清楚区分受测SHA与最终HEAD。不能预填尚未完成的结果。

## 还需完成

实际查看新版膝盖与裸模裆底的前后对照，确认没有新尖角、塌陷或穿插；必要时修改并重跑。重点Pilot Flips Switches、Snatch、Jogging、Shooting Arrow及现有起步/行走/劳动动作。当前不宣称视觉通过，也不宣称已合main。

正式自动检查与图片复核后才合main，由用户再简单视觉复核。原PR #12失败补面不合入。之后才丰富有轮廓差异的衣服和装饰，不重新把完美裸模当整个衣柜的前置条件。

## 本地体验

合并前：`git fetch origin`、`git switch fix/lowpoly-deformation-cages`、`git pull --ff-only origin fix/lowpoly-deformation-cages`、`npm ci`、`npm run dev`。
Node.js>=22.12，打开终端地址。快速审图另需一次 `npx playwright install chromium`，随后 `npm run review:local`；完整模式加 `-- --full`。合并后再切回main。截图保存在review-deformation/local，不进入源码库。
