# 固定基模与换装 V5 · 2026-09-20

## 目标

两个固定男女基模，一档标准低模，以服饰款式、发式、头饰和染色提供差异。用户明确允许删除旧代码和不兼容历史配方。Web只做外观与FBX试衣，Unity负责玩法；scale微调只是后续可能性，本轮不建设缩放编辑器。

## 已删除的路径

连续height/build、存储preset/palette、Recipe V1–V4导入迁移、旧outfit/hat/equipment查询、auto发髻、LOD0/1细分、LOD视图选项与导出参数、旧版本默认值补齐。没有保留兼容开关。旧文档与退役验收说明从当前树清理，Git历史仍可追溯。

## 新契约

Recipe V5只有6个顶层字段：version、bodyType、slots、dyes、hairStyle、hairColor。七个slots必须完整；三色必须为六位hex。未知字段、缺字段、旧版本及大于32KB的文件拒绝，当前角色保持不变。存储键wanhu.character.wardrobe.v5；不读取旧键。动作导出wanhu-target-motion版本2，bodyProfile只含固定基模id/version，不再存连续体型。

保留八套推荐、三种发式、跨男女混搭、三色与发色、部件锁定、seed随机、30步撤销、保存、导入导出、23份FBX测试库、搜索分类收藏、暂停逐帧与源骨架对照。基础农户卫兵弓手是现有资产，不是旧协议兼容层。

## 独立服装升级（2026-09-20）

V5协议保持，不另建V6。现有7种上衣和7种下装进入独立资产/装配路径；`patterns.ts`管理固定款式，`assets`提供衣裤鞋专用网格/双权重，`assembly`按显式覆盖表替换内部皮肤。旧`tailoring.ts`和复制身体衣面路径删除。

几何升级为`wanhu-modular-garments-v2`。原16组身体/绑定哈希仍保持；旧衣面表面哈希仅用于旧重构证明，本次明确退役。没有变更FBX或参考模型，不导入PR #12。人体髋裆不再是衣柜推进的阻塞前提。

首批实现短衣/长袖衣、直裤/简化阔裤与鞋的模块化，不是外部服装Mesh导入器或大量成熟礼服完成版。固定女性映射保留，尚未导入DCC男女独立衣服源文件。

## 验收

两个固定基模/单一精度；保留全部FBX动态扫描、原动作采样和误差门槛。新增明确开口/覆盖/部件独立性检查和破坏性反例，裤装贯穿扩大至全部7种下装及重点动作源键+中点。原170部件组合与3312服饰姿态检查保留（FBX增加时动态变化）。

实际图片审查后合main，由用户简单视觉复核；不录视频，保留播放结束和循环自动检查。截图涵盖裤装独立显示、混搭、浅色、无光照/线框、坐姿深蹲正侧背及同相机前后对照。真实结果见工作交接与PR，不预填成功。

## 后续

先完善成熟短衣/裤装接口和美术，再扩充领型、边饰、衣长与冠髻。需要新的服饰拓扑时新增专用资产，不恢复通用身材编辑器、实时布料或全身IK。
