# 军人与甲胄工作流

## 当前执行点：S6-6 重甲步兵整套重做

Heavy 重新定义为重甲步兵：整套重做 `heavy_armor`、`heavy_armor_skirt` 与六款既有 Heavy 头盔，不以骑乘适配为设计目标，也不修改骑乘系统。前后与侧向长甲裳连续过膝到小腿，删除旧的两段外鼓甲裤腿；胸腹、肩腰、护臂和帽壳/眉檐/护颊/护颈一起重做，而非拉长 Medium。

甲装等级仍为第一轴；驻地第二轴只拥有 palette + helmet，普通/队长仍只用实际 headwear 区分。六款 Heavy 头盔沿用原 ID 和试衣/preset 映射，独立混搭保持；Recipe V5 六字段/七槽位、固定男女、20骨、最多双权重、inverse bind、Renderer、Actor、动作/保存/染色协议不变，不恢复退役资产兼容。

当前是 S6-6 候选，不是已验收成品。完整原下身检查仍会在 Heavy 的 jogging/start-walking 中拦截裙底与小腿的穿插；不得合并 main、降低采样/容差、改接触豁免或把静态闭合通过写成动作通过。必须修模型、重跑三条正式 Actions、下载并实际查看原始 Canvas 图后再决定合并。实际 head、运行编号、截图与最终决定记录在本轮 PR，不把历史成功当作新候选成功。

沿用已有 `s6-6-heavy-infantry-rebuild` 分支建立新 PR；PR #66 已合并，不能沿用其成功记录作为新候选证据。上甲、长甲裳和六款 Heavy 头盔全部重做，Light/Medium、标准头盔、武器、坐骑、动物与动捕不改。

## 目标与数据所有权

军人第一视觉轴为甲装等级 `light / medium / heavy`，分别表达轻短灵活、标准正规军甲装、长甲裳/长战裙与宽厚沉稳的重步兵。驻地 `palace / frontier / city` 为第二轴，主要负责 palette 与头盔；身份只有 `soldier / captain`，只改变 headwear variant。重甲是一个中性、可染色的共享基底，不复制三份驻地专属近似甲。大轮廓和色块优先，不做魔幻巨肩。

`src/soldier/` 拥有试衣分类、配色和头盔选择；真实甲胄作者资产在 `src/character/wardrobe/assets/military/`，沿原封口和装配入口工作，不建立第二套生成器或 Renderer。首次从居民装进入驻地可应用该驻地默认套装；后续驻地切换不覆盖等级、混搭衣裤和装备。等级由实际 top + bottom 推导，混搭返回无完整等级；身份由 headwear 推导，不能独立保存第二份状态。

Recipe 保持 V5 六字段 `version / bodyType / slots / dyes / hairStyle / hairColor`，七槽位 `headwear / top / bottom / shoes / back / leftHand / rightHand`。不得加入 serviceStyle、armorClass、soldierIdentity、profession、role 或 rank。固定男女、20骨、最多双权重、inverse bind、Actor 生命周期、Renderer 数量、Mixamo/MediaPipe/GLB 链路、保存与染色协议不变。不增加肩甲骨、甲裙骨、动态肩甲或实时布料。

## 现役与候选资产

| 等级 | 上甲 | 下装 | 发布边界 |
|---|---|---|---|
| Light | `city_guard_brigandine` | `city_guard_trousers` | S5 轻甲原型，暂不改名 |
| Medium | `medium_armor` | `medium_armor_skirt` | S5 皇宫/边疆共享中甲 |
| Heavy | `heavy_armor` | `heavy_armor_skirt` | S6 用户审查候选，合并与美术认可分别记录 |

首次默认关系仍为皇宫/边疆中甲、城市轻甲；S6 支持独立三轴与显式重甲选择。发布状态由实际 main/PR 读取确认，不沿用旧阶段文字判断。皇宫绛红/黑铁/暗铜，边疆深靛灰/旧铁/暗赤，城市青黑/灰铁/暗金褐。队长顶饰保持细高、居中、收尖，不恢复宽短缨、肥厚后拖和横向大冠；普通/队长不另做专属衣裤、鞋或武器。六款Heavy重盔按驻地×身份在试衣/preset层映射，不增加Recipe字段；头盔可独立混搭，通用闭合/发型/权重规则保持。

S5 的 `palace_guard_armor / frontier_lamellar_armor / palace_guard_skirt / frontier_armor_skirt` 和更早的 `guard_light_armor / archer_tunic / guard_pants / archer_pants / loose_trousers / boots` 均已退役。严格文件入口拒绝旧 ID，不迁移旧 JSON，不恢复兼容工厂。旧设计查 Git 历史。驻地与头盔职责见《皇宫禁卫与长枪》《边疆戍卒与长枪》《城市守军与长枪》《普通士兵与队长》；其中历史 S1/S2 记录不是当前合法资产表。

## 分阶段执行与停止条件

本轮授权上甲、长甲裳和六款 Heavy 重盔整套重做；不改正式 Actions 调度、游戏玩法或 Light/Medium。原 S6-4 提速设计保持。不得为全绿删除检查、降低采样、放宽容差或伪造截图。交付区分本轮增量与 PR 累计草稿。

### S6-1：作者几何

Heavy 由宽厚外肩、深胸腹、护臂、腰甲和宽厚前后摆区分中甲，不靠整体缩放或延长裙摆。下装必须腰起、正背跨中线、两个真实裤管出口和单一闭合壳，不恢复六块大腿外挂板或外露黑短裤。

S6-6 候选封口后预算为上甲568 tris / 286逻辑顶点、长甲裳292 tris / 148逻辑顶点，共860 tris；普通重盔188 tris / 98逻辑点、队长重盔218 tris / 115逻辑点。身体、鞋、武器不计入这份衣甲预算。前后和侧向整圈甲裳延伸到小腿段，真实窄腿出口只在低位裙边内侧，不能恢复膝下外鼓的两只甲裤管。帽壳、厚眉檐、护颊和护颈必须比各驻地 Medium 实质更包覆；队长仍仅用细高竖向顶饰。

独立入口 `npx tsx scripts/check-soldier-heavy-authoring.ts`；正式 `check:soldier` 调用同一检查。覆盖男女×三配色、拓扑闭合连通/非流形、有限坐标、合法骨权重、预算、几何/配色独立性和轮廓反例。保留此前22个故障类别并增加高位分叉、缺前围裳、缺后围裳3个独立造型反例，总数25。通用变形检查针对新低位入口、膝圈与内侧插值保留逐点独立校验，原5个反例保留并增加4个Heavy反例。报告 `soldier-heavy-authoring.json` 遵循 SOLDIER_CHECK_DIR 和实际 REVIEW_HEAD_SHA。

### S6-2：衣柜接入

`npx tsx scripts/check-soldier-wardrobe.ts` 已验证54组轻/中/重×男女×三发型×三配色，8组单件/跨等级组合，384组随机种子组合、31个非法文件和3个装配故障。检查真实工厂、封口、衣面/权重/颜色、遮蔽皮肤和V5文件往返，不只核对目录ID。未锁定随机不生成军装，显式锁定军装必须保留。报告 `soldier-wardrobe-integration.json` 随原静态报告归档。静态文件往返不等于浏览器验收。

### S6-3：三轴交互

候选 `6d66931c76a60aff1d79c9b3d6d7eaedf81dc1a9` 的 Targeted `36187401058` / job `108244296430` 中 focused fitting browser 已 success。覆盖3个首次默认套装、18组三轴组合、混搭装备、染色所有权、性别、撤销、浏览器保存/恢复、导出/导入和暂停 jogging 相位。原完整 browser 也在前一候选 `bfe18645` 的 job `108242511167` 中完成过 success，最终候选仍须重跑。

相机比较只允许1e-10内舍入误差，保留真实位移和NaN反例；不得修改机位掩盖模型。独立入口 `node scripts/check-soldier-fitting-browser.mjs`，完整回归仍是 `npm run check:soldier-browser`，两者都必须通过。

### S6-4：真实动作与穿插

S6-6 使用十圈前后与侧向连续的长甲裳，删除外部左右甲裤管。腰臀使用 Hips/对应 Thigh，膝下采用原膝前/膝后梯度；低位内衬入口仍为真实窄腿出口。只修改作者几何和固定权重，无动作相位分支、新骨骼或运行时布料。

当前完整原下身检查本地执行18162帧，Heavy男女五动作2018帧，jogging/start-walking存在裙底连接面与小腿的阻塞交点，因此不能宣布动作合格。坐姿、射箭、Snatch的下身离散样本0阻塞不代表上肢遮挡已视觉合格，也不能覆盖行走失败。复现帧与原始报告记在《工作交接》和本轮 PR；正式 Actions 必须对实际新 SHA 重跑。

`check-tailoring-intersections.ts`、接触豁免表和所有几何容差必须保持原样。它仍覆盖男女×全部下装×原5动作的全部源键和中点，包括Snatch；旧真裙压力动作诊断边界保持，但不得给Heavy新增豁免。完整Soldier蒙皮仍覆盖18套×男女×当前所有真实动作源键和中点，不能用闭合/有限坐标检查代替穿插检查。

动作分片只拆外层甲装组合，不改帧集合、真实蒙皮函数或断言。只有三分片汇总覆盖验证成功才算完整动作通过，单个分片成功不能关闭本阶段。有限离散采样不等于任意混搭、全时域零穿插；腋下、肩甲、胸/上臂、腰胯、裙底/膝部仍需实际看图。

### S6-6：视觉与合并

恢复 PR 的 `[soldier-visual]` 标记或显式选择 Manual Visual soldier scope。使用原 WebGL Canvas，至少产出同身体/姿态/palette/机位 Light/Medium/Heavy 对比、重甲正侧背、三等级经营俯视、同Heavy换驻地配色，以及男女坐姿、jogging、shooting-arrow关键帧。补查男女Snatch关键帧，并分别输出三驻地中甲盔/重甲盔同身体同机位对比；Light/Medium/Heavy必须先读出短/标准/长裳厚重三档。不能生成假图、另画模型、临时改模型或调整相机掩盖问题。

下载Actions原始截图并实际打开，问题回到对应层修复；自动通过、截图生成、AI审图和用户认可分别记录。关键截图直接贴在对话中。最终同步AGENTS、README、工作交接与军人各篇，共同规则集中于本工作流。

## Actions 执行与加速边界

只保留 Build & Core Checks、Targeted Numeric Checks、Manual Visual Review 三条永久 workflow。删除旧 one-shot-motion-evidence 和本轮临时诊断快照；不留下用于接手下载源码的任务。保留同分支过期运行取消和所有正式失败门槛。

Targeted 的军人检查不再串行互相阻塞：

- prepare-soldier 提取/验证动作一次，生成带真实候选SHA的输入包。
- soldier 执行原静态/衣柜/故障注入及原完整下身穿插，是完整穿插的唯一执行者；原通用 tailoring 任务仍执行 check-tailoring-v2，不重复跑同一穿插脚本。专用触发范围覆盖原全部tailoring输入。
- soldier-motion 按light、medium、heavy并行，fail-fast为false，各分片保留该甲装全部驻地/身份/性别/真实动作源键和中点。
- soldier-browser 的focused和full并行，均独立保留原入口，不与几何检查互相遮蔽。
- soldier-motion-coverage 汇总三份真实报告，要求相同SHA、三分片完整不重复、逐动作时间集合摘要一致、样本与总姿态计数相等。缺分片、旧SHA、失败报告、重复/少采样等9个故障反例必须被拒绝。当前27动作应为972个组合和467100个姿态；新增动作时按真实目录重建期望，不把历史数字当减采样目标。

缓存只保存动作提取输入，不缓存测试结果。缓存key覆盖原FBX/GLB文件、提取脚本/库、动作数据定义、目录、Node主版本、lockfile和缓存动作自身；不使用宽松restore-keys。命中后仍执行原prepare:motion的源哈希/目录验证与MediaPipe检查。军人下游只恢复本次workflow、同SHA输入包，校验SOURCE_SHA和git diff后测试。Build/Manual复用同一准备入口，build关闭重复prebuild调用，但不省略动作准备。

`node scripts/check-ci-graph.mjs` 随Build运行，防止重复/漏掉重要命令、错误分片、遗漏触发范围或恢复第四条workflow。原人物、衣柜、坐骑、家畜、动捕和浏览器回归保留。提速来自去重、复用输入和缩短串行路径，不宣称整条流程必然快3倍；实际时间取决于runner排队、缓存命中和最慢任务，应从新旧Actions日志比较。

## 提交与最终发布

本轮新 PR 只有在三条正式 Actions 完成、完整数值通过、原始截图实际打开且造型合格后才可转 Ready 并决定正常合并。已知运动穿插失败必须留在 Draft，不能用用户希望 main 审查的历史授权跳过本轮停止条件。用户美术认可单独记录；Manual skipped不是视觉success，本地结果不是Actions结果，PR #66旧候选成功不能冒充新提交通过。记录真实 head、运行编号、完成项和失败项；不为等待 Actions 连续密集轮询。

最终合并前重新读取main并保留并行修改，使用expected head SHA正常merge，不强推；合并后复检main Build/Targeted，分别记录候选与main SHA。

本轮不做新武器、刀盾/弓弩兵、背旗、披风、将军、多级军阶、职业生成器、战斗AI、伤害、阵型、ECS军队、Root Motion、Foot IK、万人GPU Crowd或Unity正式士兵玩法。历史S0至S5和被否定几何查Git历史，不恢复运行时兼容。


## 史料取材与写意边界

中国国家博物馆第五届古代服饰研究论坛中，王业宏《满族入关前的服饰刍议》摘要讨论了上下分开的甲衣、甲裳。仅据此提取上下披挂的结构关系，不能扩张为全部朝代、全部重步兵的统一制式。来源：https://www.chnmuseum.cn/yj/yjxshjl/hy/fsyjlt/fslt2023/ 。

故宫博物院《月白缎绣金龙铜钉棉甲》提供上衣、下裳及分件装配的实物参考；大都会艺术博物馆《Helmet with Feather Crest（清 羽飾頭盔）》提供护颊、护颈及帽壳组合参考。来源：https://www.dpm.org.cn/collection/embroider/231859.html 与 https://www.metmuseum.org/art/collection/search/732564 。这些资料包含清代皇室/高等级用途，不能直接当作普通步兵战甲复原依据。项目只取完整包覆、分件层次和轮廓节奏，不照搬龙纹、身份装饰或羽饰，更不把 Light/Medium/Heavy 的游戏等级宣称为历史制度。
