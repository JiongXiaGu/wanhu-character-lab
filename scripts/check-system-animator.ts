import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import * as T from 'three';import {GENERATED_SYSTEM_ANIMATOR_CLIPS} from '../src/character/system-animator/catalog.generated';import {validateMotionData,type HumanoidMotionData} from '../src/character/motion/data';import {retargetMotion,exportTargetMotion} from '../src/character/motion/retarget';import {makeCharacter} from '../src/character/v3/outfit';import {BODY_TYPES,createRecipe} from '../src/character/v3/types';
assert(GENERATED_SYSTEM_ANIMATOR_CLIPS.length>0,'没有 SystemAnimator GLB 样本。');for(const def of GENERATED_SYSTEM_ANIMATOR_CLIPS){const source=JSON.parse(readFileSync(`public/system-animator/${def.id}.json`,'utf8')) as HumanoidMotionData;validateMotionData(source,def.id);assert.equal(source.source.provider,'SystemAnimator');assert.equal(source.source.format,'glb');assert.equal(source.source.profile,'system-animator-glb-v1');assert(source.bindPositions[4]>.5&&source.bindPositions[4]<1.5);assert(source.source.groundDiagnostics&&Object.values(source.source.groundDiagnostics).every(Number.isFinite));assert(Math.abs(source.fps-30)<1e-4);
 for(const bodyType of BODY_TYPES){const data=makeCharacter(createRecipe({bodyType})),bake=retargetMotion(data,source);assert.equal(bake.clip.tracks.length,21);assert(bake.rotations.every(a=>a.length===source.times.length*4&&a.every(Number.isFinite)));assert(bake.hips.every(Number.isFinite));assert(Math.max(...bake.groundLift)-Math.min(...bake.groundLift)<1e-8);assert(Math.abs(bake.rootTrajectory[0])<1e-8&&Math.abs(bake.rootTrajectory[2])<1e-8);assert(bake.hips[1]>.25&&bake.hips[1]<1.5,`首帧 Hips 高度异常：${bake.hips[1]}`);const out=exportTargetMotion(data,source,bake);assert.equal(out.bones.length,20);assert.equal(out.source.provider,'SystemAnimator');assert.equal(out.groundPolicy,'fixed-baseline-p10');const head=bake.rotations[5],first=new T.Quaternion().fromArray(head,0);let authored=false;for(let i=4;i<head.length;i+=4)if(first.angleTo(new T.Quaternion().fromArray(head,i))>.01){authored=true;break;}assert(authored,'Head 动画被错误清零');}console.log(`PASS ${def.id} · ${source.times.length} samples · male/female · fixed ground baseline`);}
