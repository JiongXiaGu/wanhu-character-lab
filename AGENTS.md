# AGENTS · 衣冠工坊 V5

## 接手

这是wanhu-character-lab的3D换装与FBX试衣项目，不是头像或UI原型项目。先核对远端最新main、任务分支、PR和Actions，再读README、工作交接、固定基模与换装V5、轻便服饰与头饰安全留量、服装生成架构、运行时人物生成架构、换装工作台使用、玩家角色自定义与服饰分期、Mixamo动画接入、男性FBX校正、GPU骨骼动画迁移契约、GitHubActions截图验收规范。不要用聊天中的历史SHA覆盖后续提交。

## 产品与数据

固定成年男性／女性两个基模，保留原LOD2单一标准低模精度。重点是写意服饰、混搭、染色和可靠动画；连续身高／胖瘦、体格模板、多档LOD和旧配方兼容已退役，Git保存历史。

Recipe V5精确包含version／bodyType／slots／dyes／hairStyle／hairColor，七槽位完整。用户文件必须经过parseRecipeFile严格验证；createRecipe只供内部构造。只读V5保存键。保留农户、卫兵、弓手可编辑资产、跨男女混搭、三色／发色、发型／头饰、种子、锁定、撤销、保存和导入导出。

正式路径：Recipe → 固定基模／资产注册／装配 → CharacterData → rig → viewport；FBX → 离线提取 → retarget → 目标局部轨道 → player。patterns只注册，assets拥有专用网格与静态权重，assembly负责固定覆盖和装配，adornments负责冠髻，headwear-fit负责一次性帽饰制作留量。没有外部服装Mesh通用导入器；不恢复旧tailoring或兼容fallback。

20骨骼语义、索引与父关系固定，每顶点最多两个非零权重；+X右、+Y上、+Z前，单位米。男女绑定位置可以不同，衣服共享该基模骨架，不另建Animator，不未经验证共享男女最终矩阵。顶点色仍参与网格缓存。

## 当前生产基线

PR #15的形变收尾、PR #16的第一批常服已经完成，不继续旧人体任务分支。第一批劳作短衣／交领常服／半臂与直筒／束脚长裤的专用资产、固定色区、袖口台阶和轮廓继续保留，不能退化为同网格换色。

第二批为真无袖背心、短袖短褂、及膝短裤、A字分片短下裳和帽发安全留量。现为9上衣／9下装、10套推荐；原ID与推荐保留。资源几何wanhu-modular-garments-v6，皮肤wanhu-skin-cage-v3，绑定wanhu-fixed-bodies-v1，Recipe仍V5。资源v6不是Recipe V6。

短装不能沿用长衣长裤的全覆盖：第二批上衣covers仅torso，下装仅pelvis／thigh，必须保留裸露上臂和小腿。短下裳明确是随两腿分开运动的裙裤式简化，不宣称连续软布短裙。头饰允许偏大；保留原发髻遮蔽规则，不缩小头发或建立按发型适配表。

## 制作边界

服装主导：先独立衣裤与固定覆盖，再扩充明显不同的轮廓，不把消除普通人体低模折面作为服饰生产前置条件。旧LOD2哈希只证明当时重构无造型回归，不永久冻结人体；真正修模任务可以调整局部拓扑、体积、双权重与法线，但必须明确任务边界、升级资源版本并重新审查，不能为衣服无限追加人体补偿。

PR #12是失败历史，不合入、不拣选、不作为V5基础。上传FBX与参考模型保留原样。允许按服装固定覆盖表不绘制内部皮肤；不叠加旧问题衣面，不按动作临时删面，不更改源动作／照明或放宽阈值伪造通过。上衣包含自己的领袖腰带，不新增无限叠穿槽；差异大的款式允许独立拓扑和固定权重。

Web只做外观与试衣。职业、导航、生产、战斗、场景交互留在Unity；不扩连续体型、缩放编辑器、儿童老人、捏脸、程序动作、实时布料、逐帧服装碰撞、全身IK或GPU群体运行时。武器仍为附件展示，精确接触与道具事件尚未完成。

## 动画与审图

自动扫描全部FBX，不写死文件数量；提取失败明确报错。采用真实inverse bind，不以首帧代替。保留头部相对真实bind的完整旋转差，不把HeadTop_End当脸前向或按动作锁定俯仰。换装／男女切换保持暂停相位，切动画复用网格。

日常先修改→本地或执行环境真实渲染→多角度截图→实际看图→继续修改，不要求每次小改都等完整Actions。优先Vite／Three.js真实浏览器。本地默认、--wardrobe、--lightwear入口保留，加--full运行各自完整矩阵。

环境无法运行浏览器时，确定性源码几何重建只作明确标注的静态辅助，不得冒充网页、WebGL、FBX动画、交互或小屏验收。工具直接打开图片失败，可以把原截图只读排入证据页再实际读取像素；必须记录这种查看方式，不能把“文件已生成／已下载”写成“全部看过”。

合并运行时代码、修改网格／绑定／retarget／FBX／播放器／测试／工作流或阶段收尾时，核对相关npm检查与原六项正式Actions：Build、Character Model Review、Mixamo Retarget Review、Wardrobe Review、Tailoring V2 Review、Modular Garment Review。两批截图矩阵都保留，不新增第七条永久流程。纯文档收尾可以引用未变化的受测代码，不重新跑视觉流程。不部署Vercel／Visual。

图片人工抽查不要求视频；原播放结束和循环自动检查保留。Pilot Flips Switches、Snatch、Jogging、Shooting Arrow、Start Walking与现有劳动／行走动作继续多视角、多相位检查；源帧／中点、算法、容差和压力动作不得缩减。短裤没有旧长裤膝下结构可以声明独立接口契约，但仍必须进入原贯穿采样，不能作为动画豁免。

记录真实受测SHA、run、结果、下载和实际查看范围、未查看的视频、已知限制与最终main SHA；自动成功、图片生成和实际看图分开。有限抽查不是所有连续时刻零穿插保证。原身体／绑定保护签名与故障注入保留，不刷新哈希掩盖意外改变。源码改动重新验证；纯文档改动比较差异后单独记录。正常合并、核对最新HEAD，避免覆盖并发提交；临时制作／证据分支不得当作新的生产基础。
