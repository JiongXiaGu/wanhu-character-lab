# AGENTS.md

接手前先阅读 Documentation/工作交接.md、项目概览.md、运行时人物生成架构.md、服装生成架构.md 和 V3验收记录.md，再检查相关代码。不要从旧对话直接猜实现状态。

## 当前正式路径

`Recipe → v3/body.ts → v3/outfit.ts → CharacterData → v3/rig.ts → CharacterViewport`

src/character/v3 的 types / cage / body / outfit 是不依赖渲染器的生成层；rig.ts 是 Three.js GPU 蒙皮适配层。历史 segmented / V2 二维挤出路径已经移除，不得误接回去。

## 不得破坏

- 连续、封闭、四边面主导的固定三维人体；不通过相交块冒充连续蒙皮。
- 身体、服装、装备均运行时生成；不改成预制 FBX / GLB 内容库。
- 20 个稳定骨骼语义，最多 2 个非零权重；动画不逐帧重建 Mesh。
- 布料模拟不属于默认方案。
- 表面替换避免完整裸体与完整衣服重复；通用隐藏裁剪仍需单独设计。
- 结构布线显示实际 face 边界，全三角模式显示全部索引边；不得只显示好看的参考线。
- 不用 DoubleSide 遮盖绕序 Bug。
- 分别统计逻辑顶点、渲染顶点、三角形和骨骼；低三角形不等于已经满足 Unity 万人性能。

## 开发与验收

用户要求连续推进到可验收阶段，不按单个文件或子步骤反复要求用户说“继续”。但不能在回复之后声称仍后台开发；只有当前执行的工具/CI 工作才是实际进度。

每次生成器/动画改动必须执行：

1. `npm run check:mesh`（包括封闭连通、索引、绕序、锚点、权重和动作采样）
2. `npm run build`
3. 浏览器真实 WebGL + 交互检查
4. 实际打开正、侧、背、3/4、结构布线及招手/屈膝/行走截图。

自动测试不证明没有自交或所有动作都不穿模。记录已检查范围、已知局限和截图对应版本。

重构在工作分支完成验证后再合入 main。不得强推/覆盖并发提交。不得主动操作 Vercel。依赖使用 package-lock.json 和 npm ci。文档和代码同步更新，不维护互相矛盾的“当前阶段”。
